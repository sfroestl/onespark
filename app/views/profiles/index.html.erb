<% provide(:title, "Profiles") %>
<div class="container-fluid">
  <h1> All profiles (<%= @profiles.count unless @profiles.nil? %>) </h1>
  <hr class="soften"/>
    <div class="span11 offset1">
      <table class="table">
        <thead>
          <tr>
            <th>#</th>
            <th>Username</th>
            <th>Forename</th>
            <th>Surname</th>
            <th>City</th>
          </tr>
        </thead>
        <tbody>
          <% for profile in @profiles %>
            <tr>
              <td><%= @profiles.rindex(profile)+1 %>
              <td><%= profile.user.username.downcase %></td>
              <td><%= profile.forename %></td>
              <td><%= profile.surname %></td>
              <td><%= profile.city %></td>
              <td width="100px">
                <%= link_to "Add to friends", friendships_path(:friend_id => profile.user_id), :method => :post %><br/>
                <%= link_to "Show", profile %>
              </td>
            </tr>
          <% end %>
        </tbody>
    </table>
  </div>
