<!-- Tasks -->
<div class="element single_content">
	<h1 class="element_heading"><%= headline %></h1>
	<div class="element_description">
		<dl>
			<dt>Last activities</dt>
				<% date = 1.day.ago(@project.tasks.first.created_at) rescue nil # increment the day
				    @project.tasks.last(10).each do |task| 
				      if date < task.created_at.to_date
				      date = task.created_at.to_date
					%>
				        <h2 style="margin-top:0.3em"><%= format_date_short date %></h2>
				    	<% end %>
				      <p>
				        <%= format_date_long task.created_at %> | created task "<%= task.title %>" by <%= task.creator.username %> </span></a>
				    </p>

				<%  end %>
			</dt>
		</dl>
	</div>
</div>