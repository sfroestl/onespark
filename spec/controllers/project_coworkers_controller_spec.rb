require File.dirname(__FILE__) + '/../spec_helper'

describe ProjectCoworkersController do
  # fixtures :all
  # render_views

  # it "create action should render new template when model is invalid" do
  #   ProjectCoworkers.any_instance.stubs(:valid?).returns(false)
  #   post :create
  #   response.should render_template(:new)
  # end

  # it "create action should redirect when model is valid" do
  #   ProjectCoworkers.any_instance.stubs(:valid?).returns(true)
  #   post :create
  #   response.should redirect_to(project_coworkers_url(assigns[:project_coworkers]))
  # end

  # it "update action should render edit template when model is invalid" do
  #   ProjectCoworkers.any_instance.stubs(:valid?).returns(false)
  #   put :update, :id => ProjectCoworkers.first
  #   response.should render_template(:edit)
  # end

  # it "update action should redirect when model is valid" do
  #   ProjectCoworkers.any_instance.stubs(:valid?).returns(true)
  #   put :update, :id => ProjectCoworkers.first
  #   response.should redirect_to(project_coworkers_url(assigns[:project_coworkers]))
  # end

  # it "edit action should render edit template" do
  #   get :edit, :id => ProjectCoworkers.first
  #   response.should render_template(:edit)
  # end

  # it "index action should render index template" do
  #   get :index
  #   response.should render_template(:index)
  # end

  # it "show action should render show template" do
  #   get :show, :id => ProjectCoworkers.first
  #   response.should render_template(:show)
  # end

  # it "new action should render new template" do
  #   get :new
  #   response.should render_template(:new)
  # end

  # it "destroy action should destroy model and redirect to index action" do
  #   project_coworkers = ProjectCoworkers.first
  #   delete :destroy, :id => project_coworkers
  #   response.should redirect_to(project_coworkers_url)
  #   ProjectCoworkers.exists?(project_coworkers.id).should be_false
  # end
end
